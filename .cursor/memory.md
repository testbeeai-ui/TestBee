# Project Memory

> Agents: read this file at the **start** of every task. Append **Decisions Log** after changes (see `.cursorrules`).

## Stack
- **App:** Next.js (App Router), TypeScript, Supabase (auth + DB), Tailwind
- **AI tutors:** Gyan++ / **Prof-Pi** — Sarvam (`lib/sarvamGyanClient.ts`), optional verifier (`PROF_PI_VERIFY=1`, `lib/profPiVerify.ts`)
- **RAG:** Modal sidecar `modal-rag/` → BGE-M3 + Supabase passages (`lib/rag.ts`, `lib/gyanBotAnswer.ts`)
- **Other:** Vertex/Gemini where used; subject chat `app/api/subject-chat/route.ts`

## Active git branch
- **`gyan++changes-4`** — Gyan++/Prof-Pi, CAS verify, Sarvam limits (push only when user asks)

## Key paths (Gyan++ / Prof-Pi)
| Area | Files |
|------|--------|
| Answer pipeline | `lib/gyanBotAnswer.ts` |
| Prompts / limits | `lib/gyanContentPolicy.ts`, `lib/gyanBotPersonas.ts` |
| Sarvam + strip thinking | `lib/sarvamGyanClient.ts` |
| Verifier | `lib/profPiVerify.ts` |
| CAS (chemistry) | `lib/casExtract.ts`, `lib/casVerify.ts`, `modal-rag/cas_verify.py` |
| Tests | `lib/sarvamGyanClient.thinking.test.ts` |
| Subject chat API | `app/api/subject-chat/route.ts` |

## Key paths (Mock test / MCQ library)
| Area | Files |
|------|--------|
| Library UI (`/mock-test`, MCQ tab) | `components/prep-mock/library/MockTestLibraryView.tsx` |
| Class 11/12 chapter lists + labels | `components/prep-mock/constants.ts` (`MCQ_CHAPTERS`, `SUBJECT_LABELS`, `subjectEmojis`) |
| Page shell | `components/prep-mock/MockPageContent.tsx`, `app/mock-test/page.tsx` |

**CBSE NCERT question JSON (local, not in repo):**  
- **XII:** `C:\Users\rentk\Downloads\CBSE XII-20260519T150236Z-3-001\CBSE XII\` — 44 chapters (Physics 15, Chemistry 16, Math 13).  
- **XI:** `C:\Users\rentk\Downloads\CBSE XI-20260519T150202Z-3-001\CBSE XI\` — 45 chapters (Physics 15, Chemistry 14, Math 16).  
Import: `CBSE_CLASS_LEVEL=11|12` + `scripts/import-cbse-12-mcqs.ts` (see `.cursor/docs/cbse-12-mcq-import.md`).

## Prof-Pi answer shape (do not mix subjects)
- **Math / chem:** `PROF_PI_STRUCTURE_CONTRACT` — **Formula / Proof / Steps / Key intuition / Exam trap**
- **Physics only:** `PROF_PI_PHYSICS_STRUCTURE_CONTRACT` — **Given / Formula / Steps / Answer** (+ optional intuition / trap); no "Wait/Let me" walls
- Verifier must not replace good drafts with meta-review text; structured drafts often skip verify

## Env (names only — values in `.env`, never commit)
| Variable | Purpose |
|----------|---------|
| `SARVAM_API_KEY` | Prof-Pi + bots |
| `SARVAM_PROF_PI_MAX_TOKENS` | Completion cap (default 2048; model hard limit) |
| `SARVAM_MAX_OUTPUT_TOKENS` | Global cap |
| `PROF_PI_VERIFY` | `1` = second Sarvam pass |
| `PROF_PI_VERIFY_MAX_TOKENS` | Verifier cap (scales with draft) |
| `RAG_SIDECAR_URL` | Modal deploy URL |
| `RAG_INTERNAL_TOKEN` | Must match Modal secret `custom-secret` |
| `DEBUG_GYAN_PROMPT_SIZES` / `GYAN_LOG_SARVAM_USAGE` | Sarvam usage logs |
| `SUPABASE_FETCH_IP_FAMILY` | Optional `4`/`6` — pin Supabase server fetch to IPv4 (dev flaky NAT) |
| `RAZORPAY_KEY_ID` / `RAZORPAY_KEY_SECRET` | Razorpay Standard Checkout (server-only secret) |
| `NEXT_PUBLIC_RAZORPAY_KEY_ID` | Razorpay checkout modal key id (browser) |

## Supabase projects (two — do not merge RAG)
| Project | Ref | Use |
|---------|-----|-----|
| **TestBee** (main) | `bytsiknhtcnlxwzgqkrd` | App, auth, migrations in `supabase/migrations/` |
| **TestBee RAG** | `yobzgdsecnutzyvuidqz` | `textbook_chunks` only — **never merge/pause/migrate** (Phase 4 Option A) |

`NEXT_PUBLIC_SUPABASE_*` → main only. `RAG_SUPABASE_*` → Modal secret → RAG project only. Runbook: `docs/architecture/supabase-cost-phase4-infra-billing.md`.

## Modal RAG
- **URL:** `https://testbeeai--testbee-rag-serve.modal.run`
- **Deploy:** `cd modal-rag` → `python -m modal deploy modal_app.py`
- **Secret:** Modal dashboard → `custom-secret` (`RAG_SUPABASE_URL`, `RAG_SUPABASE_ANON_KEY`, `RAG_INTERNAL_TOKEN`)
- **Windows:** `modal.exe` often not on PATH — use `python -m modal` or full path under `%APPDATA%\Python\Python314\Scripts\`

## Dev commands
```bash
npm run dev:turbo          # local app
npm run build              # production build check
npx vitest run lib/sarvamGyanClient.thinking.test.ts
```

## How we record changes
| Size | Where |
|------|--------|
| Quick fix / config / one file | One line in **Decisions Log** below |
| Big feature / long write-up | `.cursor/docs/<name>.md` + one-line link here |

**Long docs:** `docs/gyan/gyan++review.md`, `docs/branches/BRANCH_DESCRIPTION.md`, `docs/architecture/RESEARCH_RETROSPECTIVE.md`, `.cursor/docs/README.md`

## Do not commit
- `.env`, `.env.local`
- `data/local-cache/news-blog.local.sqlite*` (orphan local cache; was at project root)
- `.cursor/debug-*.log`
- `.next/`, `node_modules/`

## Repository layout (agents: follow every task)

See also `.cursor/rules/repo-layout.mdc` (always applied).

| Path | Purpose |
|------|---------|
| `app/`, `components/`, `lib/`, `hooks/`, `store/`, `types/` | Application code |
| `public/`, `supabase/`, `modal-rag/` | Static assets, DB migrations, Python RAG |
| `scripts/` | One-off/import tools; orphans → `scripts/legacy/` |
| `docs/` | Committed product/architecture docs (`docs/gyan/`, `docs/branches/`, `docs/architecture/`, `docs/cursor/`) |
| `docs/dev-notes/` | Transient logs, API dumps (mostly gitignored) |
| `data/local-cache/` | Local SQLite caches (gitignored) |
| `.cursor/memory.md` | This file |
| `.cursor/docs/` | Agent SQL notes, import runbooks |
| `.cursor/rules/*.mdc` | Cursor rules (`codegraph`, `karpathy-guidelines`, `repo-layout`) |
| `.cursor/skills/` | Optional skill copies (not app code) |
| `.codegraph/` | CodeGraph index (gitignored) |
| **Project root** | Next.js + package config only — no loose `.md` notes, logs, sqlite, or `__pycache__` |

**Wrong path (do not use):** `.cursor/.cursor/rules/` — nested duplicate; use `.cursor/rules/` only.

## Project root tidy (2026-05-23)
Anything that is not Next.js / tooling config now lives under:
- `docs/branches/`, `docs/architecture/`, `docs/gyan/`, `docs/cursor/` — permanent docs
- `docs/dev-notes/` — transient logs and ad-hoc dumps (`.tsc-check.txt`, `debug-*.log`, `out*.json`)
- `scripts/legacy/` — orphaned/unused scripts (e.g. `sqlite-init-stub.js`)
- `data/local-cache/` — local SQLite + other gitignored runtime caches

## Locale / content
- **Chinese (CJK)** in repo: only `.cursor/skills/*` (agent skills), not in `app/` / `lib/` / `modal-rag/`
- Student-facing copy: English (Hindi optional when student writes in Hindi)

## Key paths (Admin coupons)
| Area | Files |
|------|--------|
| Tab shell + URL state (`?tab=teacher\|student`) | `app/admin/coupons/page.tsx` |
| Teacher RDM coupon manager | `app/admin/coupons/TeacherCouponsTab.tsx` |
| Student plan coupon manager | `app/admin/coupons/StudentCouponsTab.tsx` |
| Teacher admin API | `app/api/admin/coupons/route.ts` |
| Student plan coupon API | `app/api/admin/coupons/student/route.ts` |
| Student directory (admin picker) | `app/api/admin/students/route.ts` |
| Student claim API | `app/api/user/coupons/claim-plan/route.ts` |
| Student claim UI | `components/profile/subscription/SubscriptionCoupon.tsx` |
| Coupon utils + extend logic | `lib/subscription/subscriptionCouponUtils.ts` |
| Teacher DB | `supabase/migrations/20260807000000_create_coupons_table.sql` |
| Student plan DB | `supabase/migrations/20260808120000_subscription_coupons.sql` |

## Key paths (Mobile app — Expo)
| Area | Files |
|------|--------|
| Root | `mobileapp/` (isolated from Next.js) |
| Routes | `mobileapp/app/` |
| Features | `mobileapp/features/{dashboard,lessons,gyan,chatbot,earn,edufund,news,blogs,profile,settings}/` |
| API client | `mobileapp/services/api/client.ts` |
| Supabase auth | `mobileapp/services/supabase/` |
| Providers | `mobileapp/providers/AppProviders.tsx` |
| Setup | `mobileapp/README.md`, `mobileapp/.env.example` |

## Decisions Log
- 2026-06-30: **Mobile native Google auth** — `signInWithIdToken` via expo-auth-session (no Supabase/edublast.in OAuth redirect); Google Cloud redirect URI = exp://…/auth/google.
- 2026-06-30: **Mobile app Phase 1** — Dashboard daily checklist + study streak APIs; Gyan++ Supabase feed + doubt post + Prof-Pi polling detail; Lessons curriculum from Supabase + topic content API + lesson player routes.
- 2026-06-30: **Mobile app Phase 0 scaffold** — `mobileapp/` Expo SDK 57 + TypeScript; folder layout `app/`, `features/*`, `shared/`, `core/`, `services/`, `providers/`; Supabase Google OAuth (`edublast://auth/callback`), Bearer API client to Next.js; tab shell (Home/Learn/Gyan++/Earn/You); feature stubs for all student modules; website untouched.
- 2026-06-28: **Dashboard community feed live** — replaced `MOCK_FEED_POSTS` with `RawCommunityFeed` embedded preview (`lessons_raw_posts`); links → `/explore/community`.
- 2026-06-28: **Teacher Wizard close snooze** — X close stores 1h dismiss in localStorage per teacher; auto-open respects expiry (`lib/teacherPortal/teacherWizardDismiss.ts`). — Step-2 right column shows per-student completion reward × audience + publish fee (Gyan++/mock/quiz/etc.); Concept Focus panel now includes completion reward in total. — claim runs on last advanced set submit (`isLastNonEmptyAdvancedSet`), not hardcoded set 3 (6-set quizzes never credited after set 3-only bug). — payout eligibility uses same rules as UI (incl. bits_test_attempts); task-progress GET retries grant fulfillment + returns `completionReward` status for students (`no_escrow` when pre-escrow assignments).
- 2026-06-28: **Assignment completion parity (Concept Focus / quiz / mock)** — shared `lib/classroom/assignmentStudentCompletion.ts` (matches ClassFeed done rules); `loadBundle` uses custom audience + engagement/submitted fallbacks; mock `catalog-paper-attempt` upserts task progress; ClassFeed adds mock tracking params; teacher detail modal shows Done banner + syncs concept-focus cache.
- 2026-06-28: **Teacher portal hooks fix** — moved `useUserStore` above early returns in `app/teacher-portal/page.tsx` (was after auth guards, causing Rules of Hooks violation).
- 2026-06-27: **Book live class modal — crisp minimal UI** — replaced translucent `white/8` layers + gray CTA with solid `#0c0e14`/`#080a10` surfaces, opaque `zinc-800` borders, emerald primary, disabled zoom blur on dialog. — "Other ways you earn RDM" lists all live tracks: teacher/student referral, bulk invite, live delivery, quality bonus (+200), Gyan++ (`gyan_teacher_answer_rdm`). — removed stale "Content co-creation +20" / "Teacher Section upvotes +30"; Refer & earn now shows live Gyan++ reward (`gyan_teacher_answer_rdm`); wallet sidebar duplicate removed. — applied `teacher_free_assignments_quota` + `starter_unlimited_assignments` on TestBee main (`bytsiknhtcnlxwzgqkrd`); verified `rdm_config` keys (free=10, starter=9999, pro=9999, publish charge=10). — active Pro teachers skip `teacher_create_assignment_rdm` on publish (server-enforced in charge/refund APIs); Free/Starter still pay per publish. Wizard button shows "included on Pro".
- 2026-06-27: **Starter unlimited assignments (charge per publish)** — `teacher_starter_assignments_per_month` default 9999 (no monthly cap); every publish still deducts `teacher_create_assignment_rdm`. Free stays 10/mo cap. Migration `20260627130000_starter_unlimited_assignments.sql`.
- 2026-06-27: **Free teacher assignment quota (10/mo, charge not reward)** — free tier now gets `teacher_free_assignments_per_month` (default 10, admin-tunable); publish still deducts `teacher_create_assignment_rdm` (default 10 RDM) per assignment; cap exceeded → upgrade CTA (Starter/Pro). Migration `20260627120000_teacher_free_assignments_quota.sql`; wizard shows monthly quota + publish charge. — `/api/classrooms/explore` now returns safe public teacher profile fields (bio, subjects, exam tags, teaching levels, location/qualification/experience/verification status), and `/classrooms` explore cards show a clean top-right profile icon as the hover/focus target. Hover opens a right-side text-first teacher profile preview (no photo/avatar/icon inside the panel) so students can inspect basics before expressing interest. No private email/phone/doc links exposed.
- 2026-06-26: **Admin RDM investor card polish** — Teachers cards in `/admin/rdm-table` now show an investor-readable one-line summary per config (`summary` in `TEACHER_RDM_ADMIN_META`), while technical wiring (`Config key`, `Teacher UI`, `Server`) moved behind a compact hover/focus `!` tooltip to reduce card height and make each block understandable at a glance.
- 2026-06-26: **Admin RDM — teacher growth rewards surfaced** — moved teacher referral (signup/paid/window) + bulk-invite (flat/min/per-student/window) keys out of generic "Earn & Learn (Referrals)" group into a new "Growth rewards" subsection in the Teachers block of `/admin/rdm-table` with proper titles/teacher-surface/server-path metadata; added optional `unit` to `TEACHER_RDM_ADMIN_META` (+ `TEACHER_RDM_ADMIN_GROWTH_KEYS`) so day/student-count keys no longer mislabel as "RDM". No DB change (all 30 teacher keys already exist in prod & were already editable). Audit confirmed every implemented teacher RDM rule is admin-connected.
- 2026-06-26: **Teacher RDM/charge model investor audit** — verified spec vs shipped: referral signup/paid (+500/+500), bulk invite (+5000/+100, RPCs), live-class delivery (100+10×cap50), quality bonus (+200 credit-only) are LIVE & admin-tunable in `/admin/rdm-table` (`rdm_config`). NOT built: Expert-Teacher 1.5× multiplier + trailing-10 badge, recorded-class +300/+500 viral, Gyan++ 10-upvote/causal bonuses. Divergent by design: assignments are a charge+quota (not +50 reward); Pro live cap 12 (not unlimited); single 30 students/class cap (not 30/100/tier). Report: `docs/teacher/teacher-rdm-investor-report.html`.
- 2026-06-26: **Wizard: no Calendar series** — Step 3 name-only for all tiers; slot booking after launch; `normalizeTeacherPlanTier` requires future `teacher_plan_expires_at` for paid; recurring API still 403 for free.
- 2026-06-26: **Wizard section gating (free vs paid)** — Step 3: free teachers save batch name only (no schedule/repeat UI); Starter/Pro get Google Calendar series form; recurring API returns 403 for free; `canUseGoogleCalendarSeries` in `teacherPlan.ts`.
- 2026-06-26: **Teacher subscription management UI** — status card (days left, renewal date, billing period bar), cancel/resume auto-renewal APIs, `teacherSubscriptionBilling.ts`; plan limits API returns `subscription` block.
- 2026-06-26: **Book live class modal close** — footer "Close window" removed; round × icon top-right in header (still no outside/Escape dismiss).
- 2026-06-26: **Book live class modal polish** — `BookLiveClassSlotPanel` compact modal: stacked fields, duration pills, gradient header + quota badge, `WallTimeSelects size="md"` (roomier, no chevron overlap); still closes only via "Close window".
- 2026-06-26: **Live-class quality bonus RDM (credit-only)** — students rate a section schedule occurrence (Path A) 1–5★ after it ends; teacher earns a flat **+200 RDM** only when (1) the ratings window closed, (2) quorum holds (≥5 raters AND ≥50% of roster), and (3) the Bayesian-smoothed score (`(sum+m·prior)/(n+m)`, m=8, prior=4.0) ≥4.5. **Never debits** — low ratings = no bonus, no penalty, no claw-back; idempotent via `quality_awarded_at`; monthly cap (20) guards the economy. New `lib/teacherPortal/liveClassQualityRdm.ts` (+test, 14/14), migration `20260929120000_teacher_live_class_quality_rdm.sql` (`live_class_ratings` table, quality columns on `teacher_section_schedule_rdm_grants`, RPCs `submit_live_class_rating` / `get_pending_live_class_rating` / `award_teacher_section_schedule_quality_rdm` / `award_eligible_teacher_live_class_quality_rdm`, 8 `rdm_config` keys). Student star popup `components/classroom/LiveClassRatingPrompt.tsx` (mounted on `app/classroom/[id]/page.tsx`) + APIs `POST /api/classroom/live-class/rate`, `GET .../pending-rating`, `POST /api/teacher/section-schedule/award-quality-rdm`; teacher hook `useTeacherLiveClassQualityRdmAwards` wired beside delivery hook in `app/teacher-portal/page.tsx`; admin keys in `TEACHER_RDM_ADMIN_META`. Doc: `.cursor/docs/teacher-live-class-quality-rdm.md`. **Migration applied to TestBee prod (`bytsiknhtcnlxwzgqkrd`) 2026-06-26** via `apply-linked-migrations.js`; `integrations/supabase/types.ts` regenerated (`live_class_ratings` + 4 RPCs in types).
- 2026-06-25: **Classroom invite join UX** — `/join?code=` shows teacher-invite copy (not join-request); auto-links bulk invites on sign-in via `link_my_classroom_invites`; lookup API returns teacher name.
- 2026-06-25: **Site tour investor revamp** — tour is now self-contained (no background `router.push`, fixes jerk + blogs companion flicker); per-slide cosmetic RDM counts up in header as 5s elapses (`creditCurrentFlat`), real reward stays single +100; verification removed (reading = done); X opens a "Hold on!" confirm overlay (No stay / Yes close) instead of minimizing; resume FAB removed; `completeSiteTourCarousel` now bulk-marks all `ONBOARDING_REWARD_TASK_IDS` complete before claiming.
- 2026-06-24: **Site tour resume FAB** — compass icon + spring entrance, breathing glow ring, hover/active polish, badge pop (reduced-motion safe) on close.
- 2026-06-24: **Site tour** — hidden "Open task — earn RDM" link in carousel modal.
- 2026-06-24: **Site tour auto-advance + route sync** — 5s RAF timer with pause/resume; on menu change `syncBackgroundRoute` pushes onboarding href (modal stays open); header tag follows `contextPathname`.
- 2026-06-24: **Site tour = investor HTML exact** — `SiteTourCarousel` uses full `siteTourCarouselData.ts` (10 menus, 37 features); layout order header → menu pills → content → bottom carousel → footer; CSS ported from `edublast_tour_carousel.html`; flat navigation via `siteTourCarouselFlatNav.ts`; onboarding OK/claim unchanged.
- 2026-06-24: **Onboarding hydrate loop fix** — `hydrateOnboardingProgressFromServer` skips no-op merges/events; site tour fetch once per user; companion skips redundant `setStepsProgress`. — Dashboard (blue theme) + RDM Wallet use investor `feat-row` + feature snapshot from `edublast_tour_carousel.html`; `SiteTourInfoSlideBody.tsx`. — Carousel: 11 sections (Dashboard + RDM intro, merged Prep+Mock 10 RDM); per-task RDM in `onboardingChecklistRdm.ts` (90 across slides + 10 completion bonus = 100); info slides OK without server verify. — Split `import type` from value imports in `OnboardingRewardDialog.tsx`; `SiteTourCarouselHost` uses `SITE_TOUR_ONBOARDING_TOTAL_STEPS`; moved `ONBOARDING_REWARD_TASKS` to `lib/onboarding/onboardingRewardTasks.ts` to break circular import with `freeTrialClient` / `onboardingNextTask` (vitest suite fix). (`reviewAt`, Know It soft-hide, Tomorrow → next 9AM local, promote to New); wired store, dashboard Memory Recall, InstaCue player; Unsure hidden from dashboard, revision Unsure tab only; tests `lib/saved/revisionCardRecall.test.ts`.
- 2026-06-23: **Agent git push rule** — `.cursor/rules/git-push-approval.mdc`: never push to GitHub without explicit user approval in the conversation. — `quizBankSetRows` used `[2,3]` only; fixed to `ADVANCED_QUIZ_BANK_SET_INDICES` (2–6). Tab label shows set count (`Quiz (6 sets)`) not raw MCQ total when multi-set. — `standardRazorpayCheckoutDisplayConfig()` (UPI first, `show_default_blocks: true`, no hide); optional `RAZORPAY_CHECKOUT_CONFIG_ID` on orders.
- 2026-06-22: **Prof Pi launcher drag** — top/left positioning (v3 storage), cursor-grab follows pointer anywhere on viewport; hover label non-interactive.
- 2026-06-21: **Razorpay checkout config fix** — removed custom `config.display` (`show_default_blocks: false`) from `RazorpayCheckoutButton`; it caused Razorpay `iin`/netbanking 401 without dashboard `checkout_config_id`. Default checkout UI + order_id-only amount now. — live `www.edublast.in/api/create-order` returns Razorpay `Authentication failed` (bad/mismatched `RAZORPAY_KEY_*` on Vercel, not missing env). Added `lib/razorpay/razorpayEnv.ts` (quote trim), `GET /api/razorpay-health`, clearer 502 message on key rejection.
- 2026-06-21: **Homepage footer Privacy Policy link (Google OAuth crawl)** — `FOOTER_LINKS` in `components/landing/landing-constants.ts` had `Privacy` → `#` (dead anchor). Updated to `Privacy Policy` → `/terms-conditions/privacy-policy` and `Terms` → `/terms-conditions/terms-and-conditions`. Homepage already renders `LandingFooter` via `app/LandingPageClient.tsx`.
- 2026-06-21: **Question bank UX (free)** — free users see Set 1 only + locked “Question bank →” upsell; sets 2–3 hidden until Starter/Pro.
- 2026-06-21: **Lesson references gated to Starter/Pro** — free/trial users get `TopicReferencesUpgradeDialog`; paid users see Recommended References modal (`hasTopicReferencesAccess`). — `isPublicDeepLinkTarget()` + one-shot navigate ref in `LandingPageClient`; Razorpay checkout.js loaded once via `lib/payments/loadRazorpayCheckoutScript.ts` (stops duplicate Script tags).
- 2026-06-21: **Razorpay wired into subscription flows** — Profile `SubscriptionCheckout` + teacher RDM `SubscriptionsView` use real `RazorpayCheckoutButton`; `POST /api/create-order` supports `purpose: subscription|rdm_pack`; `POST /api/user/subscription/activate-after-payment` + hardened `coupons/purchase` verify HMAC before DB writes. Test/live mode = env keys only (`RAZORPAY_KEY_*`, `NEXT_PUBLIC_RAZORPAY_KEY_ID`).
- 2026-06-20: **Razorpay Standard Checkout (standalone)** — `npm install razorpay`; `POST /api/create-order` + `POST /api/verify-payment` (Node runtime, env `RAZORPAY_KEY_*` / `NEXT_PUBLIC_RAZORPAY_KEY_ID`); reusable `components/payments/RazorpayCheckoutButton.tsx`; demo at `/razorpay-demo`. Existing simulated subscription checkout unchanged. — DB: `profiles.subject_chat_regional_language` + RPC `set_subject_chat_regional_language`. UX: pick from header dropdown (no blocking modal) → AlertDialog confirm → EN + locked regional only. Server: `resolveSubjectChatLanguage`. Pricing: `EN + 1 regional`. Migration: `supabase/migrations/20260918120000_profiles_subject_chat_regional_language.sql`.
- 2026-06-18: **Lesson chat multilingual restored to Pro-only** — `resolveSubjectChatLanguage` + `/api/subject-chat` enforce `access.multilingual`; `SubjectChatbot` locks regional langs (Lock + upgrade copy); pricing badges Free/Starter `EN only`, Pro `5 langs`. Gyan++ stays English-only. — global `@deeplake/hivemind` + `hivemind cursor install` wires user-level hooks (`~/.cursor/hooks.json` → `~/.cursor/hivemind/bundle/`); not an app dependency. Repo: `npm run hivemind:install|status`, runbook `docs/cursor/hivemind-setup.md`. Restart Cursor after install. Does not affect `npm run build`.
- 2026-06-13: **KCET past-paper images re-hosted in our own Supabase Storage bucket** — the 19 imported KCET papers (`past_paper_questions.question_html` / `solution_html`) used to embed `<img src="https://www.testbee.in/preview/show_qimage/<id>.<ext>">` pointing at a legacy Testbee server outside our control. While the legacy endpoint still returns 200, depending on it for student-facing question images is a fragility footgun. Created the public-read Storage bucket `past-paper-images` (1 MB cap, image/png + image/jpeg) on `bytsiknhtcnlxwzgqkrd`. `scripts/migrate-past-paper-images.ts` walks the 3,315 KCET question rows, downloads each of the 748 distinct legacy URLs (concurrency 8, 3 retries), SHA-256 hashes the bytes, uploads to `past-paper-images/<hash>.<ext>`, and rewrites the `src=` in both `question_html` and `solution_html`. `scripts/verify-past-paper-images.ts` confirms 0 legacy refs remain and HEAD-checks all 749 unique Supabase URLs (all 200). `scripts/import-past-paper-json.ts` now also calls `selfHostImages()` from `scripts/pastPaperImageHost.ts` on every `question_html` / `solution_html` it writes — so future BITSAT / JEE Main / KCET imports self-host on the fly (per-process URL cache dedupes). The OCR-malformed case (`<img src="...1584770336.png  Te">` with junk glued onto the filename) is handled in both the migration script and the importer helper. Equivalent SQL kept in `supabase/migrations/20260613130000_past_paper_images_bucket.sql` (the migration history is out of sync on this project so the bucket was created via the supabase-js admin client).
- 2026-06-13: **BITSAT exam-size correction — `question_count` and `total_marks` now reflect real exam totals (150 pre-2022, 130 from 2022)** — `scripts/import-past-paper-json.ts` previously set `question_count = batch.length` (i.e. PCM-only count of 100/125), hiding the 25–30 English/Logical Reasoning questions our taxonomy does not cover. BITSAT 2022+ reduced the paper from 150 to 130 questions, so the UI was doubly wrong (missing both the real size and the post-2022 reduction). Fix: `examQuestionCount = questions.length` (full source JSON count), `examTotalMarks = examQuestionCount * 3` (full 150/130 × +3). Re-imported all 17 BITSAT papers. New DB state: 2009-2021 → 150 q / 450 marks; 2022-2024 → 130 q / 390 marks; Solved Set → 38 q / 114 marks. Imported PCM rows unchanged (124-126 for pre-2022, 99-100 for 2022+). KCET was already correct (no English/LR; 180/240 questions all imported). Log fields renamed: `exam_question_count` / `exam_total_marks` (full exam) and `questions_inserted` (PCM-only in app).
- 2026-06-13: **BITSAT PYQ parser hardened — all 1,955 PCM questions imported** — `scripts/import-past-paper-json.ts`. Previously 14 questions (~0.7%) failed parsing due to source-typo cases; all now resolved. Two structural upgrades: (1) **Contiguous-run detection** in `extractStemAndOptionsAbcd` / `…Flexible` — first look for the A→B→C→D run that *starts a `<p>` block* (skipping stray `(c)` mentions in stems), then fall back to any contiguous run. (2) **`extractMalformedLabelMcq` last-resort fallback** — when no extractor matches, split on `<p>` blocks, take the first 4 markers in the option region (accepting `o./O.` and `n)/N)` as OCR typos for `a`/`c`), and re-number A–B–C–D in document order. Logs a `⚠️` line per recovered question with a note. Rejected 1 question (BITSAT 2023 Q107219, match-the-columns with `<table>` layout — explicitly not an MCQ). DB state verified: every paper's `actual = question_count` from source. Files: `scripts/import-past-paper-json.ts` (untracked, ~600 lines).
- 2026-06-13: **KCET 2023 — 240 Qs / 240 marks / 240 min (no PCMB override)** — user wants 240 minutes not 320. Removed the `if (examName === "KCET" && coveredSet.has("biology")) durationMinutes = 320` block from `scripts/import-past-paper-json.ts` and re-imported all 19 papers. The KCET 2023 card still carries the amber "Dual Stream (PCMB)" badge (4 subjects covered) so users know the 240 Qs bundle PCM + PCB variants. Verify: `KCET 2023 count=240 actual=240 marks=240 dur=240`, 0 mismatches across all 19 papers.
- 2026-06-12: **Investor HTML** — platform optimization before/after (phases 0–6, no financial framing): `docs/architecture/platform-optimization-phases-0-6.html`.
- 2026-06-13: **Investor dashboard mockup v3 (polished)** — `docs/investor/edublast-dashboard-investor.html`. Redesigned `edublast_dashboard_v3.html` as an investor-grade preview: gradient cover header with eyebrow + accent title, 4-card pitch strip (Daily active loop / Engagement signal / Personalization / Community flywheel), mac-style browser frame around the live dashboard, refined dashboard chrome (sticky blurred topnav, gradient logo + avatar, accent sidebar, refined card borders, polished typography, icon-gradient avatars), and "What this screen matters / What an investor should look at first" 2-column narrative footer. All v3 interactions preserved (4 flyouts, clickable 7-day grid, 5-task checklist with progress bar, 3-post community feed with vote/save/thread, filter pills, 5-row leaderboard, 3 upcoming mocks with Start now, 4 trending topics).
- 2026-06-11: **Supabase cost Phases 0–6 summary** — before/after doc `docs/architecture/supabase-cost-phases-0-6-before-after.md`.
- 2026-06-11: **Supabase cost Phase 6** — topic hub UX: multi-level display bundle, `"-"` placeholder handling, `TopicHubOverviewSections`, admin coverage `/admin/topic-hub`; runbook `docs/architecture/supabase-cost-phase6-topic-hub-ux.md`.
- 2026-06-11: **Supabase cost Phase 5** — opt-in Upstash site-presence buffer; admin cache 15min + warm cron; dwell monthly partitions; MCQ chapter cached API; runbook `docs/architecture/supabase-cost-phase5-scale-architecture.md`.
- 2026-06-11: **Supabase cost Phase 4 (Option A)** — keep TestBee RAG `yobzgdsecnutzyvuidqz` separate permanently; defer Pro/`auth.edublast.in`; branch policy documented; billing checklist in `docs/architecture/supabase-cost-phase4-infra-billing.md`.
- 2026-06-13: **BITSAT PYQ import (17 papers / 1,944 PCM questions)** — new `scripts/import-past-paper-json.ts` (sibling of `import-mock-paper-json.ts`) writes PYQ JSONs to `past_papers` / `past_paper_questions` with `paper_type='pyq'`, `exam_name='BITSAT'`, +3/-1 BITSAT marking, 180 min. Five option extractors: (A) `extractStemAndOptionsAbcdFlexible` 2021+ paragraph-aware with parens, (B) `extractStemAndOptionsAbcd` classic `(a)(b)(c)(d)` parens, (C) `extractStemAndOptionsAbcdDot` BITSAT 2021+ `<p>a. ... b. ...</p>` with mandatory dot, (D) `extractStemAndOptions124` numeric `(1)(2)(3)(4)`, (E) `extractImageOnlyMcq` last-resort image-only fallback (options in `<img>`, not text). `resolveSubject` explicitly returns `null` for non-PCM `subjectName` (`english`, `logical reasoning`, etc.) so they don't sneak in via the `lastSubject` carry-over. English / Logical Reasoning skipped (not in our taxonomy). **Idempotent** — re-running overwrites via slug/title match + delete. 14 questions (~0.7%) remain unparsed due to source data corruption (duplicate option letters, typos like `o.` instead of `a.`, missing option markers) — these are unfixable without manual correction in the source. Papers: 2009–2024 (16 years) + 1 Solved set; subjects_covered = [physics, chemistry, math].
- 2026-06-13: **Mock + Past papers library exam filter** — replaced Subject chips (All / Physics / Chemistry / Math) on the Past papers and Mock papers tabs with Exam chips (All / KCET / BITSAT / JEE Main). Quick mock tab untouched (still uses Subject focus). New `LibraryExamFilter` + `paperMatchesExamFilter` exported from `lib/mock/mockPapersCatalog.ts`; new `exam: string` field on `MockPaper` / `PastPaper` types (`types/index.ts`) sourced from Supabase `exam_name`. CBSE XI/XII mock papers (89) only show under "All" since their `exam_name` is not KCET/BITSAT/JEE Main. Search now also matches `paper.exam`. Filter wiring: `components/prep-mock/MockPageContent.tsx` (state + useMemo) → `components/prep-mock/library/MockTestLibraryView.tsx` (UI). Codegen scripts updated to match new prop shape.
- 2026-06-13: **BITSAT / JEE Main / KCET library card label** — `past_papers.class_level` and `mock_papers.class_level` are `NOT NULL` and constrained to `IN (11,12)` (CBSE-style), so the 17 BITSAT PYQ rows stay at `class_level=11` to satisfy the constraint + existing class-level filter (`paper.classLevel <= userLevel`). To avoid the misleading "Class 11" label on national-test papers, `MockTestLibraryView.tsx` renders `12th standard` for `paper.exam` in `{'BITSAT','JEE Main','KCET'}` and `Class N` for everything else (e.g. CBSE XI/XII mocks). DB data + importer (`import-past-paper-json.ts`) unchanged.
- 2026-06-13: **Subject-chat RAG quality bump** — 10 passages per question (was 5), retriever always runs all 3 passes (was short-circuit on pass 1) for broader grounding. TS: `RAG_MATCH_COUNT_SUBJECT_CHAT = 10` (`lib/gyanContentPolicy.ts`). Python: `MAX_CHUNKS_TO_SEND = 10` + always-on 3-pass merge in `modal-rag/retriever.py`. Prof-Pi / student-bot RAG untouched (still 5 chunks). Requires Modal re-deploy (`cd modal-rag && python -m modal deploy modal_app.py`) for the retriever changes to take effect.
- 2026-06-11: **Supabase cost Phase 3** — buddy Realtime 4→1 table, 50s/8min polls; ClassFeed paginated (40); site-presence uses user RLS + DELETE policies; teacher portal poll 120s; runbook `docs/architecture/supabase-cost-phase3-connections-realtime.md` (Supavisor = dashboard step).
- 2026-06-11: **Supabase cost Phase 2 complete** — RLS initplan on 11 hot tables, 6 indexes dropped / 5 added, prune RPC + cron verified 200, admin analytics cache on all routes; phase2b migrations applied live; runbook `docs/architecture/supabase-cost-phase2-db-hygiene.md`.
- 2026-06-11: **Supabase cost Phase 1 quick wins** — IST-once reconcile opt-in, 50s presence heartbeat, profile Realtime debounce, subject-chat RAG cap 5, retriever skip multi-pass; runbook `docs/architecture/supabase-cost-phase1-quick-wins.md` (Modal redeploy for retriever).
- 2026-06-11: **Supabase cost Phase 0 crons** — removed all `vercel.json` cron schedules (no auto jobs in prod); routes remain for manual/external trigger; runbook `docs/architecture/supabase-cost-phase0-crons.md`.
- 2026-06-11: **Supabase cost audit** — `docs/architecture/supabase-cost-audit-2026-06-11.md` (live MCP + repo; methodology section; 250 migrations not 258).
- 2026-06-10: **Supabase OAuth branding (Step 1)** — use custom domain `auth.edublast.in` (CNAME → `bytsiknhtcnlxwzgqkrd.supabase.co`) so Google shows EduBlast domain not `*.supabase.co`; requires Pro + Custom Domains add-on (not enabled yet as of CLI check); runbook `docs/cursor/supabase-auth-custom-domain-step1.md`.
- 2026-06-10: **Waitlist UX** — no static mobile hint under phone field (silent `sanitizeMobileInput` only); after Step 1 “Join the waitlist” succeeds, auto-select `student` (or `?role=` from URL) and open ambassador modal — no extra “Register as Ambassador” click.
- 2026-06-08: **Waitlist refinements** — cleared `waitlist_submissions` test rows; sequential IDs from `EB-2026-200` via `lib/waitlist/waitlistId.ts`; duplicate email returns `alreadyRegistered` in UI except test inboxes (`michaelkillgta@gmail.com`, `testbeeai@gmail.com`, `mailidpwd@gmail.com`); ambassador urgency copy uses soft phrasing; waitlist emails use `www.edublast.in` + `join@edublast.in` footer.
- 2026-06-06: **Waitlist two-step funnel** — `/waitlist` Step 1 = email+phone quick signup (`signup_tier=waitlist`); Step 2 optional ambassador full form upgrades same row; migration `20260808120000_waitlist_signup_tier.sql`; admin WaitlistTab shows tier badge. **Waitlist emails** — Step 1 confirmation + Step 2 application receipt via `sendWaitlistEmails.ts` + `waitlistEmailTemplate.ts`; preview `scripts/send-waitlist-preview.ts`. — `subscription_coupons` table + `profiles.subscription_expires_at`; admin generates Starter/Pro + N months (public or per-student email); student claims at Profile → Subscription → Coupon code (`POST /api/user/coupons/claim-plan`); extend stacks months on remaining time; `normalizePlanTier` honors `subscription_expires_at`.
- 2026-06-05: **Admin coupons modularized** — `/admin/coupons` split into `TeacherCouponsTab` (full manager: list, filter, generate, redeem tracking) + `StudentCouponsTab` (premium coming-soon placeholder); unified `Tabs` shell in `page.tsx` with `?tab=teacher|student` URL sync via `router.replace`; production `npm run build` OK.
- 2026-06-04: Posts RLS recursion — cycle was `posts` policy → `teacher_motivation_rdm_grants` → `posts`; fixed with SECURITY DEFINER helpers + `row_security = off` (`20260806140000`: `student_has_active_grant_for_assignment`, `teacher_owns_motivation_post`, `student_can_read_post_via_teacher_nudge`).
- 2026-06-04: Posts RLS — students can read assignment posts linked from a teacher nudge (`relatedPostId` + `targetStudentIds`) or `teacher_motivation_rdm_grants`, even when section-history rules would hide them; migration `20260806120000_posts_read_linked_assignment_via_nudge.sql`.
- 2026-06-04: Teacher motivation RDM (policy A) — `POST /api/teacher/motivation/send` charges teacher when assignment-linked bonus (`rdmDelta × students`); `teacher_motivation_rdm_grants` pending until student completes all visible tasks on `relatedPostId`; fulfillment on task-progress / generated-test-attempt; non-linked nudges message-only (no grant/charge).
- 2026-05-30: Daily streak task sync — each t1–t6 completion PATCHes `sync_free_trial_daily_streak_task` into `profiles.free_trial_daily_streak` (partial `task_ids` + `tasks.{id}.completed_at`); claim RPC now requires all 6 on server; migration `20260730120000_free_trial_daily_streak_task_sync.sql`.
- 2026-05-30: Daily streak reopen reliability fix — removed stale session-level reopen dedupe gates for Day 2 tasks `t1/t2/t3` so each fresh flow completion dispatches checklist reopen after 5s; flow-only credit path unchanged.
- 2026-05-26: Site tour checklist UI — `OnboardingRewardDialog.tsx` sticky-board layout (10 notes + task detail drawer); same backend task IDs and completion hooks; no scroll list.
- 2026-05-23: Cursor layout — moved `karpathy-guidelines.mdc` to `.cursor/rules/`; added `repo-layout.mdc`; human doc at `docs/cursor/karpathy-guidelines-setup.md`; removed root `cursor.md` and `.cursor/.cursor/rules/` nest.
- 2025-05-20: Migrated to Vertex AI SDK
- 2026-05-20: Prof-Pi physics uses **Given/Formula/Steps/Answer** contract; math contract unchanged; verifier skips meta-review and strips "Wait/Let me" narration.
- 2026-05-20: Modal RAG deployed at `https://testbeeai--testbee-rag-serve.modal.run`; Windows CLI: `python -m modal deploy modal_app.py` or `& "$env:APPDATA\Python\Python314\Scripts\modal.exe" deploy modal_app.py`.
- 2026-05-20: Branch `gyan++changes-4` — Gyan++/Prof-Pi, CAS verify, Sarvam limits pushed (`0fa59b9`, `027c801`, `3dca479`, `8113095`).
- 2026-05-20: Added `.cursorrules` — agents must read this file and append Decisions Log after significant changes.
- 2026-05-20: Chinese (CJK) text only in `.cursor/skills/*` (9 marketing/China skill docs); zero in `app/`, `lib/`, `components/`, `modal-rag/`.
- 2026-05-20: Memory policy — small changes → Decisions Log only; big changes → `.cursor/docs/<topic>.md` + one-line link (see `.cursorrules`).
- 2026-05-20: Expanded `.cursorrules` + `memory.md` with stack, paths, env names, git/deploy conventions.
- 2026-05-20: Mock-test **MCQ's** tab — `SUBJECT_LABELS` (Physics / Chemistry / Mathematics); Class 12 physics + **Communication Systems**, full semiconductor chapter title; UI in `MockTestLibraryView.tsx` (no raw `physics`/`chemistry`/`math` labels).
- 2026-05-20: CBSE XII NCERT MCQ bank on disk (44 JSON chapters) — path recorded above; align `MCQ_CHAPTERS[12]` with `examSetName` when importing.
- 2026-05-20: CBSE Class 12 MCQ — migration `20260520143000_cbse_class12_mcq_catalog.sql` deployed; import `scripts/import-cbse-12-mcqs.ts` **44/44 OK**, **4175** questions into `mock_papers`/`mock_questions` on `bytsiknhtcnlxwzgqkrd` (see `.cursor/docs/cbse-12-mcq-import.md`).
- 2026-05-20: CBSE Class 11 MCQ — same script with `CBSE_CLASS_LEVEL=11`; **45/45 OK**, **4095** questions; catalog upserted at import time; fixed duplicate **Thermodynamics** (physics `p11-12` vs chemistry `c11-6`) via `FOLDER_CHAPTER_ALIASES`. Log: `.cursor/cbse-11-import-log.txt`.
- 2026-05-20: Mock **MCQ's** tab UX — `McqChapterBrowser.tsx`: one subject at a time (tabs), chapter list only for active subject; chapter click → confirm → scrollable **preview all MCQs** (`McqChapterPreview.tsx`, `fetchCbseChapterMcqs.ts`) — not NTA next/prev exam.
- 2026-05-20: **MCQ's** library tab + preview — `profile.role === "admin"` only (`MockTestLibraryView` + `MockPageContent`); non-admins cannot see tab or `?tab=mcq`.
- 2026-05-20: Admin granted **mailidpwd@gmail.com** (`profiles.role` + `user_roles`); migration `20260521160000_add_admin_mailidpwd.sql`, script `scripts/grant-admin-by-email.ts`.
- 2026-05-20: Fixed TS2307 after partial `lib/gyan/` refactor — canonical Prof-Pi at `lib/gyan/bot/gyanBotAnswer.ts`; shared modules stay at `lib/*` (not `lib/gyan/bot/gyanContentPolicy` shims). Close stale `lib/gyanBotAnswer.ts` tab.
- 2026-05-24: Onboarding **Prep + Mock · Classes** — `lib/onboarding/prepClassesOnboardingFlow.ts`; reward popup Go → `/mock?onboarding_prep_classes=1` with violet **View all** hint on `ClassesSection`; tap → `/classrooms` + checklist complete via `maybeMarkPrepClassesOnboardingFromExplore()`.
- 2026-05-24: Onboarding **Earn & Learn · Buddy** — not marked on popup Go; `maybeMarkEarnBuddyOnboardingFromBuddyActivation()` when `buddy_invites.status = accepted` for inviter (`GET /api/buddy/state` → `hasBuddyInviteActivated`).
- 2026-05-24: Onboarding **Profile** — not marked on popup Go; `isStudentProfileBasicInfoComplete()` + `maybeMarkProfileOnboardingFromBasicInfo()` when Basic information required fields saved (`StudentProfilePersonalHub` save + profile page load).
- 2026-05-27: Site-tour checklist UI now **merges** `localStorage` onboarding progress with `profile.onboarding_reward_progress` (`getMergedOnboardingProgress`), and **hydrates** server data with a union-merge so stale profile + successful local sync cannot show “1/10” while tasks are done in-browser.
- 2026-05-27: Checklist **progress parsing** normalizes truthy flags from storage/DB; **Magic Wall** row also completes when all 3 substeps are true (`maybeCompleteCompanionTaskFromSteps`); site-tour dialog **re-syncs on pathname** while open.
- 2026-05-27: **Lessons /explore-1** — "Save changes" reappears when changing chapter picks after first unlock (`showChapterPickBanner`); first unlock still shows lock row until max chapters selected.
- 2026-05-27: Onboarding reward progress merge hardened — `getMergedOnboardingProgress` now unions local + profile flags; `/api/user/onboarding-reward` now parses truthy legacy flags (`true` / `"true"` / `1`) to prevent checklist regressions after quiz/task completion.
- 2026-05-27: **prep_mcq** Task Companion — added emerald clock “next action” row in `FloatingTaskCompanion.tsx` (same pattern as Earn & Learn Challenge) so step 4 guidance + time pill always shows under the checklist.
- 2026-05-29: Replaced layout positions of elements inside `ebc-phase-banner` in `EduBlastChallengeCard.tsx` (DailyDose card banner). Moved the "Answer options unlock in" text (`ebc-phase-sub`) to the top in small font, the active phase countdown timer (`untilChoicesSec`) to the center as the **big digital countdown timer** (e.g. `00:11` syncing with the SVG loader ring), and the "READ-ONLY PHASE" tag (`ebc-phase-label`) to the bottom in small font. Adjusted margins in `app/globals.css` to preserve correct spacing.
- 2026-05-29: Resolved 3 active ESLint warnings/errors: (1) Removed unused `savedChapters` local variable in `explore-1/page.tsx`, (2) Deferred synchronous `setDashboardClock` in `StudentHomeDashboard.tsx` via `setTimeout(..., 0)` to prevent cascading render cycles, and (3) Added `time_travel_offset_ms` to `OnboardingProfileFields` type inside `freeTrialClient.ts` to allow type-safe direct access and eliminate type-unsafe `any` cast.
- 2026-05-29: Disabled automatic popup triggers for `"Today's checklist"` (the dialog with items a, b, c, d, e shown in the second image) inside `StudentHomeDashboard.tsx` by turning `tryAutoOpenDailyChecklist` into a no-op. The checklist popup will no longer auto-open on home visits or after the 30-second onboarding cooldown, but can still be opened manually by clicking on the dashboard strip.
- 2026-05-29: Upgraded the `"Daily tasks — Day 2 of 10"` detail drawer in `OnboardingRewardDialog.tsx` with a premium dual-button design matching the onboarding detail drawer. Implemented `handleOpenDailyTask` which navigates the user straight to each task's page (e.g. `/play`, `/explore-1`, `/mock-test?tab=mcq`, `/doubts`, `/refer-earn?tab=challenges`, `/news-blog`) and automatically launches the corresponding step-by-step onboarding guide companion to guide them. Added a `useEffect` event listener for `ONBOARDING_PROGRESS_EVENT` that automatically synchronizes and checks off daily tasks when their respective activities are completed.
- 2026-05-29: Fixed absolute positioning layout clipping/overlap bugs inside the `OnboardingRewardDialog.tsx` detail drawer by limiting `.drawer`'s height to `max-height: calc(100% - 20px)` and converting it to a vertical flex container, and making `.dbody` scrollable via `overflow-y: auto; flex: 1;`. Removed redundant secondary `"Mark as done"` and `"Open page again"` buttons (since students can already manually check rows off via the circular list checkboxes directly on the main daily checklist popup, as shown in the second image), leaving a single clean premium CTA button at the bottom of the drawer (`Open task — earn +X RDM!` or `Done — Open again`). This keeps the drawer perfectly compact, visually beautiful, and eliminates any vertical scrollbars on desktop screens.
- 2026-06-02: Fixed inactive-penalty reconcile cadence in `lib/dashboard/studyDaysClient.ts` — run `reconcile_inactive_day_penalties` once per IST day (keyed by `today`) instead of once per browser session, so penalties apply without requiring a hard refresh after midnight.
- 2026-06-02: Added student-facing inactive-penalty notice wired to real reconcile results: `/api/user/study-days` now returns `{ reconcile: { penaltiesApplied, totalDeducted } }`; dashboard + profile show a supportive toast only when a deduction is actually applied.
- 2026-06-03: **New-user welcome email** — `lib/email/newUserWelcomeTemplate.ts`; `POST /api/user/login-notification` sends welcome once (`profiles.welcome_email_sent_at`, migration `20260803120000`) for accounts &lt;14d old, then login confirmation for returning students; triggers unchanged (`useAuth` sign-in, OAuth callback, OTP verify).
- 2026-06-03: **Admin email volume** — `transactional_email_logs` + `EMAIL_DAILY_SEND_CAP` (default 500/IST day); `/admin/emails` + Overview card; `GET /api/admin/email-logs`.
- 2026-06-04: **Trial-end decision gate** — `TrialExpirationGate` in `app/providers.tsx` blocks all student routes (z-index overlay) until card submit (`claim-bonus`) or **Continue with Free** (`exit-trial-to-free`); `shouldShowTrialExpirationOverlay` closes when `trial_original_ended_at` + `plan_tier=free`; exit API clears `free_trial_activated`.
- 2026-06-04: **Settings feedback survey** — investor form in `EduBlastFeedbackForm.tsx`; DB-only via `POST /api/platform-feedback` → `platform_feedback_submissions` (migrations `20260804125000`, `20260804130000`); admin inbox `/admin/feedback` — **no email on submit** (welcome/login emails unchanged).
- 2026-06-04: **`/performance` plan gating** — Performance nav always visible; **free/free_trial** click → Starter upgrade card only (no stats); **starter** → full page, category block → Pro; **pro** → full access.
- 2026-06-13: **Removed `biology` from the canonical `Subject` type union (type-only cleanup)** — `types/index.ts` `CORE_SUBJECTS` is now `["physics", "chemistry", "math"]` (was `PCM + biology`). Comment updated: "PCM only — biology removed 2026-06-13". This is the single source of truth — every import of `Subject` (30+ files across `app/`, `components/`, `lib/`) cascades to `"physics" | "chemistry" | "math"` automatically. Touched 5 call sites that had literal `"biology"` references that became excess-property errors: `components/prep-mock/constants.ts` (removed two `biology: []` chapter arrays in `MCQ_CHAPTERS[11/12]` + the `biology: "🧬"` emoji + the `biology: "Biology"` `SUBJECT_LABELS` entry), `components/prep-mock/library/MockLibraryHistorySheet.tsx` (`SUBJECT_ORDER` array), `components/prep-mock/MockPageContent.tsx` (URL-param `?subject=` whitelist), `components/prep-mock/nta/NtaExamShellMobile.tsx` (`SUBJECT_LABEL` record), and `lib/mock/mockTestAttemptTypes.ts` (`subjectBreakdownFromSection` subject list). `tsc --noEmit` after the change shows **zero new errors**; the only remaining 2 errors are pre-existing and unrelated (`lib/email/emailService.test.ts` missing `phone` field in test fixtures). Left untouched per user "type only" scope: `scripts/import-past-paper-json.ts` (5 references — script), `lib/mock/{catalogQuestionMap,mockPapersFromSupabase,pastPapersFromSupabase}.ts` (s==="biology" runtime guards — these become dead-code TS2367 warnings but compile fine since the typecheck already passed), `app/admin/users/[id]/page.tsx` (admin flair allowlist, separate string union), `modal-rag/retriever.py` (Python RAG label), `lib/gyan/bot/gyanBotAnswer.ts` (Prof-Pi forbidden-list), all SQL migrations (historical). `streakChecklistSuppressed`). New state only: `openIconFlyout: IconFlyoutId`, `activeFeedFilter: FeedFilterId`, `feedVotes`, `checklistTaps`, plus one `useEffect` that closes any open flyout on outside-`[data-icon-flyout]` click. Added 4 new Lucide imports (`Coins`, `Heart`, `BarChart3`, `Bookmark`, `MessageCircle`, `ArrowUp`/`Down`, `TrendingUp`, `ChevronRight as ChevronRightIcon`, `AlertTriangle`, `Info`, `ExternalLink`, `Check`, `Clock`) and 4 module-level constants (`MOCK_TRENDING_TOPICS`, `FEED_FILTERS`, `MOCK_FEED_POSTS`, `IconFlyoutId`/`FeedFilterId` types). Structure: (1) greeting row + 4 icon-panel buttons (RDM earned / EduFund / Streak-performance / Subject accuracy) each opening a positioned `data-icon-flyout` dropdown with stat rows; (2) optional slim trial-reward claim banner; (3) hero zone — Daily activity tracker (existing 7/30-day heatmap + bonus/penalty pills) beside a slim Today's checklist card with tappable items + RDM pill and "Open full checklist" button that opens the existing Dialog (preserved verbatim); (4) lower zone — Community feed card (4 filter pills, 3 mock posts with vote/save/thread buttons, subjects tag) + right column (Leaderboard, Upcoming mocks with "Start now" → router.push, Trending topics). FreeTrialPromoDialog / OnboardingRewardDialog kept at the root. `tsc --noEmit` shows only the 2 pre-existing `Record<Subject, …>` biology errors (lines 220/296 in the constants above the JSX); zero new errors.
- 2026-06-13: **Today's-checklist click-to-mark is admin-only** — `components/dashboard/StudentHomeDashboard.tsx`. The card on `/home` previously let **any user** click a row to flip the green tick and inflate the progress bar via local `checklistTaps` state, which would let a normal student self-claim the onboarding RDM without doing the activity. Now: the row is only `cursor-pointer` for `isAppAdmin === true` (already declared at line 329 via `useIsAppAdmin()`); the `onClick` is gated to early-return for non-admins; the `tapped` local state is treated as `false` for non-admins (so even leftover localStorage taps don't inflate the bar); `checklistTapCount` is `0` for non-admins; tooltip on each row clarifies: "Auto-tracks as you do the activity" for students, "Admin: click to mark complete (testing override)" for admins. The full-checklist `Dialog` (in the same file, ~line 2395) was already display-only — no changes needed there. `tsc --noEmit` and linter are clean (only the 2 pre-existing `emailService.test.ts` errors).
- 2026-06-14: **Removed the "Collapse" toggle from the dashboard sidebar** — `components/dashboard/DashboardSidebar.tsx`. The top-of-rail `<button>` (which read "Collapse" with a `PanelLeftClose`/`PanelLeftOpen` icon and toggled `pinned` via `togglePin`) is gone. Sidebar is now pure hover-expand (`.eb-sidebar:hover` from `globals.css`) with no visible click affordance. The `pinned` state + `localStorage` persistence + `data-pinned` attribute + `aria-expanded` are kept (so the rail can still be pinned open via `data-pinned="true"` if injected, e.g. for screenshot/keyboard purposes), but the `setPin` / `togglePin` helpers and the unused `PanelLeftOpen` / `PanelLeftClose` Lucide imports were removed as dead code. The `hydrated` flag is kept (avoids flash-of-expanded on first paint) and `void hydrated` suppresses the no-unused-var rule. `tsc --noEmit` clean (only the 2 pre-existing `emailService.test.ts` errors). ESLint clean.
- 2026-06-14: **Community feed: upvote/downvote → single "Relate" (Heart) button matching Gyan++** — `components/dashboard/StudentHomeDashboard.tsx`. The Gyan++ community feed in `components/explore/CommunityFeed.tsx` uses a single `Heart` "Relate" pill (pink-600 hover) instead of an upvote/downvote pair. Mirrored that on `/home` for visual consistency. Replaced the `<ArrowUp>` upvote + `<ArrowDown>` downvote pair on every `MOCK_FEED_POSTS` row with one `Heart` pill. State renamed `feedVotes: Record<number, number>` → `feedLikes: Record<number, boolean>` (toggle, not counter). Click toggles `setFeedLikes((s) => ({ ...s, [i]: !s[i] }))`. Default state shows grey "Relate" with pink hover; when liked: `fill-current` heart, pink-500/15 background, `text-pink-600` (light) / `text-pink-300` (dark), label switches to "Related". `aria-pressed={liked}` on the button. Removed unused `ArrowUp` and `ArrowDown` Lucide imports. Save/Thread/Subject badges untouched. `tsc --noEmit` clean (only the 2 pre-existing `emailService.test.ts` errors). ESLint clean.
- 2026-06-15: **Phase 2 dwell rolling partitions** — `prune_empty_dwell_partitions(12,6)` migration applied; 60→19 child partitions, 154→113 public tables, **1,210 dwell rows preserved**; cron wired. Detail: `.cursor/docs/supabase-table-audit.md`.
- 2026-06-15: **Exam plan icon panel** — `components/dashboard/StudentHomeDashboard.tsx`. The 3rd icon panel in the greeting row was "Streak" (showing `N days` + a "Performance snapshot" flyout with study-streak stats). Per the investor-mockup feedback, replaced it with an **"Exam plan"** panel that shows the user's target exam (JEE+KCET etc.) and opens an "AI Preparation calendar" flyout mirroring the investor mockup. New `IconFlyoutId` variant `"examPlan"` (replaces `"streak"`). New `JEE_MAIN_TARGET_DATE_ISO = "2027-01-22"` constant at module top; the flyout computes `daysToJeeMain = ceil((target - now) / 1 day)` from the user's clock (recomputes every minute via the existing `dashboardClock` `useMemo`) so the value is always live. Reads `profile.target_exam` to show the user's actual exam (falls back to `"JEE Main + KCET"` when the profile hasn't set one). Reads `classLevel` (from `profile.class_level`) to show the current class (`PUC 1` / `PUC 2`). Flyout row labels: "Target exam", "Current class", "Days to JEE Main" (amber, `~NNN days`). CTA link: `Open AI Calendar → /mock?tab=ai-calendar` (matches the existing AI Calendar tab in Prep + Mock). Removed dead `studyStreakPending`, `studyStreakReady`, `activeDaysThisMonth`, and `BarChart3` import (only used by the old streak flyout). Note: `streakDays`, `streakSummary`, `inactivePenaltyRdm`, `monthDays`, `studyStreakBonusDays/Rdm`, `trialDayNumber`, `isOnboardingRewardClaimed`, `waitingForDay2`, and `checklistDoneCount` are **still used** by other UI (trial banner, daily activity tracker streak pill, "0 of 5 done" checklist copy, the streak "don't break it" pill in the greeting row) — left intact. `tsc --noEmit` clean (only the 2 pre-existing `emailService.test.ts` errors). ESLint clean.
- 2026-06-15: **Migration squash (prod-safe)** — Single baseline `20260915120000_baseline_schema_from_prod.sql` from prod schema dump; 255 files archived to `scripts/legacy/migrations/pre-squash-2026/`; prod marked via `schema_migrations` insert only (no SQL re-run). Runbook: `.cursor/docs/migration-squash-runbook.md`.
- 2026-06-15: **Removed admin Topic hub coverage** — deleted `/admin/topic-hub` page + `/api/admin/topic-hub-coverage`; sidebar link removed. Student lessons/topic_content/explore unchanged.
- 2026-06-18: **Prof-Pi multilingual rendering fixes (Phase 1)** — CAS was already correct; broken Telugu/Hindi display came from LLM raw-Unicode math + missing output sanitizer. (1) `lib/gyanContentPolicy.ts`: new `PROF_PI_LANGUAGE_CONTRACT` (match student script: HI/TE/KN/TA/EN) + `PROF_PI_MULTILINGUAL_LATEX_CONTRACT` (all math in `$...$`, no raw √²Δ outside, no duplicate formulas, space before `$`). (2) `lib/gyan/bot/gyanBotAnswer.ts`: inject both contracts into rephrase + full-RAG system prompts; removed Hindi-only language line. (3) `lib/gyan/sanitizeProfPiMultilingualOutput.ts`: dedupe consecutive `$...$` / math fragments, wrap `x²`→`$x^2$`, unicode math→LaTeX in prose, Indic↔`$` spacing; wired in `formatSarvamAssistantReply` (`lib/sarvamGyanClient.ts`) and client safety net `components/doubts/DoubtMarkdown.tsx`. Tests: `lib/gyan/sanitizeProfPiMultilingualOutput.test.ts` (7/7). No Modal redeploy.
- 2026-06-18: **Multilingual CAS verification in Prof-Pi (Way B header translation)** — regional-language (Hindi/Kannada/Telugu/Tamil) math doubts now trigger SymPy CAS verification and render fully localized headers. (1) `lib/casExtract.ts`: `detectOperation` gained a language-independent symbol fallback (`\int`/`∫`→integrate, `\lim`/`lim`→limit, `d/dx`/`dy/dx`/`\frac{d}{d`→differentiate, `=`/`≤`/`≥`→solve, any other LaTeX→evaluate) so detection no longer depends on English keywords; `shouldRunCasVerification` simplified to the math/physics gate + `detectOperation(...) !== null`; Answer/Formula `findSectionContent` regexes loosened to tolerate an emoji/space prefix (`\*\*[^A-Za-z*]*...`); new exported `translateEnglishHeadersToRegional(text)` detects script by Unicode range and swaps English bold header keywords (Formula/Steps/Answer/Key intuition/Explanation/Exam trap/Given/Solution/Proof) for localized terms while preserving emoji prefix + colon (no-op for English). (2) `lib/gyanBotPersonas.ts`: added a MULTILINGUAL HEADER CONTRACT telling Prof-Pi to keep header keywords in English even when answering in a regional language. (3) `lib/gyan/bot/gyanBotAnswer.ts`: applies `translateEnglishHeadersToRegional(bodyOut)` as the last step before the `doubt_answers` insert (after verifier/formula/CAS all run on English headers) — 0ms/$0 post-processing. emoji structure contracts in `gyanContentPolicy.ts` left unchanged. Tests: new `lib/casExtract.test.ts` (9/9 pass). Full suite: only pre-existing unrelated failures remain (`emailService.test.ts` phone fixtures, date-dependent `dashboardTrialPopups.test.ts`).
- 2026-06-16: **Supabase server fetch IPv4 pin (opt-in)** — `lib/supabase/supabaseNodeFetch.ts` undici `Agent` now takes a `connect` option (`autoSelectFamily: true`, plus `family` when `SUPABASE_FETCH_IP_FAMILY` is `4`/`6`). Fixes dev `ECONNRESET`/`TLS reset`/`ConnectTimeout` to `*.supabase.co` over flaky Jio NAT64/IPv6 (requests were succeeding only after 49–87s retries). Set `SUPABASE_FETCH_IP_FAMILY=4` in `.env` + restart dev; prod unchanged unless var set.
- 2026-06-18: **Pricing card layout** — shared `components/pricing/PricingPlanCard.tsx` + `lib/subscription/{planCardKeyFeatures,pricingCardTheme}.ts`; fixed-height header/price/description zones, compact label+value key features, removed `min-h-[580px]` dead space; `/pricing` + subscription hub use same card. Expanded feature copy: `Magic Wall` (not monthly attempts), lessons `N chapters per subject` (no PCM list), mocks badge `N mocks` (no free-tier cap text) via `patchPlanItemBadge.ts`.
- 2026-06-18: **Lessons Subject Chat → Prof-Pi parity** — extracted `lib/gyan/profPiQualityPipeline.ts` (verifier, formula cross-check, CAS, header translation) shared with Gyan++; `lib/gyan/subjectChatProfPi.ts` + `sarvamChatCompletionThread`; `/api/subject-chat` uses Prof-Pi prompts/quality (no similar-doubt); generic follow-ups skip heavy quality; timeout 55s.
- 2026-06-18: **Lessons chat language picker fix** — Kannada/Hindi/Tamil/Telugu now honored on all plans: `resolveSubjectChatLanguage` no longer gates on `multilingual`; `SubjectChatbot` always sends selected `language`; upgrade copy is daily-limit only; pricing badges `5 langs` for subject chat.
- 2026-06-18: **Lessons chat Kannada root-cause fix** — `PROF_PI_LANGUAGE_CONTRACT` (“match question language”) was overriding UI Kannada when questions were English; subject chat now uses `getProfPiUiLanguageContract` + user-message wrap; `responseLanguage` flows through verifier/CAS retries; language persisted in `localStorage` (`edublast-chatbot-language`).
- 2026-06-18: **Lessons chat Kannada enforcement pass** — Sarvam often ignored regional prompts; `lib/gyan/ensureProfPiRegionalOutput.ts` detects English-only prose and runs a dedicated translate pass (preserves LaTeX + English section headers); wired at end of `runProfPiQualityPipeline` when `responseLanguage` is set.
- 2026-06-18: **Gyan++ English-only Prof-Pi** — public doubts reverted to strict English (`PROF_PI_GYAN_LANGUAGE_CONTRACT`); removed multilingual menu/phrases/translate pass from `gyanBotAnswer`; regional enforcement gated to `source === subject_chat` only. Lesson chat multilingual unchanged.
- 2026-06-18: **Ask a Doubt dialog stuck on step 2** — `AskDoubtDialog` resets on close/success; never persists/restores loading step; stale duplicate-RPC guard; Cancel on checking step; post errors return to step 1.
- 2026-06-18: **Pro lesson chat admin language reset** — `RotateCcw` beside language dropdown for app admins (`useIsAppAdmin`); `POST /api/admin/subject-chat-regional-language-reset` clears own `subject_chat_regional_language` lock for QA; `resetSubjectChatRegionalLanguage` in `subjectChatClient.ts`.
- 2026-06-18: **Lessons chapter hub investor v2** — `components/explore/ChapterHubInvestorView.tsx` (collapsible why-study, topic/subtopic tree + panel deep-links, progress/weightage sidebar); wired in `app/explore-1/page.tsx` for `isDetailedUnitView`; `buildSubtopicPanelPath` in `lib/curriculum/topicRoutes.ts`; scoped `.chapter-hub-v2` styles in `globals.css`. Topic/subtopic lesson routes unchanged.
- 2026-06-18: **Chapter hub topic name link** — topic title in syllabus tree navigates to topic overview (`buildTopicOverviewPath`); expand/collapse stays on number, sub-topic count, and chevron only.
- 2026-06-18: **Chapter hub navigation level** — topic/subtopic links and Start chapter from chapter hub use `advanced` only (not basics).
- 2026-06-18: **Subtopic label ellipsis** — `truncateSubtopicPreviewLabel` in `subtopicTitles.ts`; chapter hub tree + Topic Quiz card truncate long names with `…` (full text on hover).
- 2026-06-18: **Advanced quiz set gating (Question bank)** — Set 1 free for all; sets 2–3 grouped under Question bank (Starter/Pro); tap Question bank unlocks sets 2–3 in-place on the lesson card (`topicQuestionBankAccess.ts`); no redirect to mock-test.
- 2026-06-18: **Topic Quiz card copy** — removed helper paragraph above Question bank button (sets list + button carry the UX).
- 2026-06-18: **Numerals regenerate marketing UX** — free/free_trial: Regenerate button → premium upsell (Starter & Pro); starter/pro: disabled “Coming soon”; admins keep real regenerate.
- 2026-06-18: **Admin save limits (bits + numerals)** — `saved_bit_limit` / `saved_formula_limit` per plan in `rdm_config` + `/admin/subscriptions` (Lessons & Revision); enforced in `saved-content` API + client pre-check on quiz/numerals save (`savedContentSaveLimit.ts`). Migration: `20260618120000_saved_bit_formula_limits_config.sql`.
- 2026-06-19: **Lessons chapter lock off (investor)** — all chapters open for free/trial (`PLAN_LOCKS.lockChapters: false`, defaults `-1`); premium inside lesson panels (quiz Question bank, numerals, saves). Migration `20260619120000_lessons_chapter_limit_investor_open.sql`.
- 2026-06-19: **Chapter hub progress stats** — sidebar wires `bits_test_attempts` + `subtopic_engagement` via `buildChapterHubActivityStats` (`lib/curriculum/chapterHubProgressStats.ts`): **Sets taken** (advanced `||set:N` keys, `taken/total`) + **Cards created** (InstaCue `flipped` on advanced); profile refresh on tab visible in explore-1.
- 2026-06-19: **Question bank free-tier upsell** — tap Question bank on free/trial shows inline premium message + **Upgrade →** (`TOPIC_QUESTION_BANK_UPGRADE_PATH` → `/profile?section=sub-plans`); no silent redirect on first tap (matches numerals upsell pattern).
- 2026-06-19: **Question bank set list gating** — free users see single locked **Question bank** row (no Set 2/3 or Q counts); Starter/Pro see full set breakdown.
- 2026-06-19: **Topic overview level pills admin-only** — Basic / Intermediate / Advanced switcher hidden for learners (advanced-only routing); admins keep preview pills with Admin badge.
- 2026-06-20: **Saved questions plan cap** — `saved_question_limit` in rdm_config (free 20 / starter 200 / pro unlimited); enforced in `/api/user/saved-questions` + client pre-check on Mock bookmark & explore QuestionCard. Admin: Lessons & Revision field. Migration `20260620120000_saved_question_limit_config.sql`.
- 2026-06-20: **Legacy `/preview` redirect** — middleware sends `/preview` → `PREVIEW_AUTH_PATH` (`/preview-raknas-amu`) to fix 404 on old bookmarks.
- 2026-06-21: **InstaCue card scroll** — fixed long front/back answer scroll in `RevisionCard` (`flex flex-col` + `min-h-0 overflow-y-auto` on content area; stopPropagation on scroll region).
- 2026-06-21: **InstaCue revision save dedupe** — stable `rev-*` save ids for AI cards + fingerprint dedupe in save/merge/GET/POST (`lib/saved/revisionCardIdentity.ts`); InstaCuePlayer index/key fixes so deck navigation shows distinct cards.
- 2026-06-21: **InstaCue typography hierarchy** — card question/answer larger; subtopic label smaller/muted below (Revision player + lesson `RevisionCard`).
- 2026-06-21: **Topic Quiz investor card v3** — `TopicQuizInvestorCard` matches `topic_quiz_card_v3.html` (Sub-topic quiz label, title, Physics pill + unit.sub topic tag, Set 1 play row, Open Question Bank + inline premium upsell); re-verified after accidental InstaCue-shell regression.
- 2026-06-22: **Prof Pi subject chatbot UI** — `ProfPiChatTrigger`, `ProfPiChatHeader`, `ProfPiSuggestedChips`, dark markdown in `globals.css`.
- 2026-06-22: **Memory Recall page scroll** — removed in-card scroll (`overflow-y-auto`/`touch-pan-y`); wheel/touch over cards scrolls the page; tap still flips.
- 2026-06-22: **Topic Quiz investor v3 + InstaCue colors** — single-card layout kept; palette uses `edu-card` / theme tokens (matches InstaCue tab), not custom purple hex.
- 2026-06-22: **Lesson InstaCue card metadata** — hide Concept/subject/subtopic pills on `InstaCue.tsx` `RevisionCard` only; `/revision` `InstaCuePlayer` unchanged.
- 2026-06-22: **Revision recall Tomorrow clock** — `useRecallNowMs` + `refreshDueRevisionCards` promote due cards at 9 AM local without full reload; wired in dashboard, revision page, InstaCuePlayer; persist rehydrate promotes stale tomorrow rows.
- 2026-06-22: **InstaCue Tomorrow grace** — midnight–1:59 AM local → due same day 9 AM; from 2 AM → next day 9 AM (`getRevisionTomorrowDueMs` in `revisionCardRecall.ts`).
- 2026-06-22: **Production API/DB plan** — `.cursor/docs/production-api-db-optimization-plan.md` (saved-content bulk POST, duplicate GETs, subtopic_engagement JSONB, phased fixes).
- 2026-06-22: **Phase 0 API/DB optimizations** — saved-content GET cache (60s TTL), debounced full sync (3s) + fingerprint skip, `PATCH /api/user/saved-content/revision-cards` for recall taps, batched InstaCue read PATCH, daily telemetry prune cron in `vercel.json`.
- 2026-06-22: **Phase 1 API/DB optimizations** — incremental `user_saved_items` upsert+diff delete (no bulk wipe), stop profiles JSONB dual-write on saved-content POST/PATCH, GET profiles fallback when items empty, subtopic engagement 204 skip-unchanged + 1.5s debounce, cap check memoized per POST.
- 2026-06-23: **Phase 2 API/DB optimizations** — saved-content GET `?types=` + ETag/304; `student_subtopic_engagement` + `student_bits_attempts` (`20260811120000`); `review_at` column (`20260811120100`); routes table-first; InstaCue due badge; `scripts/backfill-user-saved-items.ts`. Deploy: `node scripts/apply-linked-migrations.js --phase2` (see migration-squash-runbook).
- 2026-06-23: **Supabase fetch resilience** — 30s total budget on server fetch, 12s middleware / 25s client API abort, site-presence heartbeat in-flight guard, 503 on upstream errors. Optional `.env`: `SUPABASE_FETCH_IP_FAMILY=4` for flaky IPv6.
- 2026-06-23: **Phase 2.5 revision card slim rows** — `review_at` on `user_saved_items`, slim JSON on write + hydrate on read (`revisionCardRowPayload.ts`); removed redundant MockPageContent saved-content fetch.
- 2026-06-23: **Prod migration apply (new only)** — applied 4 pending local migrations via `scripts/apply-linked-migrations.js` (save limits, investor lessons open, saved-question cap, subject-chat regional language); all 8 files in `supabase/migrations/` now synced on remote. `db push` still blocked by archived pre-squash history — use apply script for new SQL.
- 2026-06-23: **saved-content GET 500** — ETag embedded fingerprint with control chars → `ERR_INVALID_CHAR`; `savedContentWeakEtag` now uses SHA-256 digest; `useAuth` sync catches fetch failures.
- 2026-06-23: **InstaCue recall UX** — toast feedback for Tomorrow/Know it/Unsure (`getRevisionRecallFeedback`); flip state resets on card change in InstaCuePlayer + dashboard flip cards; cursor-pointer on action buttons.
- 2026-06-23: **Topic quiz card scores** — re-applied per-set score sublabels + green check after submit; hide Open Question Bank after unlock (`TopicQuizInvestorCard` + lesson page).
- 2026-06-23: **Dashboard Memory Recall flip** — single flipped card per grid; hover or flip another card unflips the previous (`DashboardMemoryRecallPanel`).
- 2026-06-23: **Hide Advanced level in learner UI** — `formatDifficultyLevelForUi` / `isDifficultyLevelShownInUi` in `lib/slugs.ts`; topic sidebar, revision badges, deep-dive titles, dashboard/explore/magic-wall copy.
- 2026-06-23: **Student profile setup UI** — `StudentProfileSetupCard` matches `edublast_profile_setup.html`; wired into `app/onboarding/page.tsx` (dark `#0E1117` page, KCET exam target, teal CORE subjects, gradient Continue).
- 2026-06-23: **Telemetry POST dedupe** — client throttle on site-presence (48s), study-days presence flush (22s gap), learning-dwell/presence (55s/85s); stable `useLearningDwellTelemetry` scope key stops effect restart storms.
- 2026-06-24: **Legacy site-tour dashboard button** — optional "Open Site Tour checklist" below Memory Recall (`SHOW_LEGACY_SITE_TOUR_BUTTON`); opens existing `OnboardingRewardDialog` unchanged for investor HTML swap later.
- 2026-06-24: **Day-1 carousel site tour** — `SiteTourCarousel` (investor `edublast_tour_carousel.html` UI) auto-opens on trial Day-1; completion marks all 10 onboarding tasks + claims +100 RDM via existing RPC; resume FAB on left; legacy sticky-note tour behind optional button.
- 2026-06-24: **Carousel tour reset + timer** — header Reset clears localStorage progress; 5s bar is read-only (no auto-credit); only "OK, got it!" marks a slide done.
- 2026-06-24: **Carousel claim buddy fix** — full Day-1 bulk merge skips `earn_buddy` invite verification so carousel completion can claim +100 RDM.
- 2026-06-24: **Carousel global overlay** — site tour popup + resume FAB follow all AppLayout routes (removed `/home`-only auto-open guards).
- 2026-06-24: **Carousel route context** — pathname maps to tour section (`siteTourCarouselRoutes.ts`); on Magic Wall etc. auto-opens that menu + "you're here" banner.
- 2026-06-24: **Carousel onboarding tasks** — 10-task tour uses shared `onboardingRewardTasksData` numbered steps; OK verifies server task completion; Open task launches checklist flow; claim requires real backend completion.
- 2026-06-25: **Carousel always starts fresh** — `SiteTourCarousel` hydration ignores saved `ci`/`earned`; every open begins at slide 1 with 0 RDM and counts up to exactly 100 (10 menus × 10), so header never opens mid-tour with leftover RDM.
- 2026-06-25: **Carousel countdown bar smooth** — progress bar width now driven via `progFillRef` DOM write in rAF (was per-frame `setProgressPct` state re-rendering the whole modal → stutter/freeze); seconds text re-renders only on whole-second change (`lastSecRef`).
- 2026-06-25: **Teacher live class delivery RDM (prod)** — applied migration `teacher_live_class_delivery_rdm` to TestBee (`bytsiknhtcnlxwzgqkrd`); roster-based bonus, no join required; verified grant 110 RDM with 0 joins.
- 2026-06-25: **Delivery RDM → section schedule (Path A)** — migration `teacher_section_schedule_delivery_rdm` on prod: new `teacher_section_schedule_rdm_grants` keyed by `(section_id, occurrence_at)`; auto-award scans ended section/Google Calendar occurrences; extra `live_sessions` (Schedule Live Session) no longer eligible. Local `20260928120000_teacher_section_schedule_delivery_rdm.sql`. UI: My Classroom section RDM labels, admin section grant table, API `POST /api/teacher/section-schedule/award-delivery-rdm`.
- 2026-06-25: **Site-tour +100 one-time-ever** — migration `20260919120000_site_tour_reward_claimed_ever.sql` adds `profiles.free_trial_checklist_reward_claimed_ever` (not cleared by reset) + guards `claim_free_trial_checklist_reward` RPC (re-claim after reset re-marks claimed_at, grants 0). API exposes `claimedEver`; dashboard shows "Site Tour / Tour claimed" card left of RDM earned. **Applied to TestBee prod** (`bytsiknhtcnlxwzgqkrd`) via Supabase MCP.
- 2026-06-25: **Dashboard Site Tour badge** — `+100 Site Tour` icon hidden when `isFreeTrialPeriodEndedForProfile` (14-day trial over).
- 2026-06-25: **Site tour carousel replaces legacy checklist** — removed root auto-open of 10-card `OnboardingRewardDialog`; `SiteTourCarouselHost` opens investor carousel right after free-trial activation (`shouldAutoOpenSiteTourCarousel`); post-task promos + claim CTA route to carousel.
- 2026-06-25: **Site tour explicit claim** — completion screen no longer auto-claims; shows “Claim {100} RDM” button → on success “Site Tour completed — thank you!” + “OK, go to dashboard” (`claimCompleted` state in `SiteTourCarousel`). Failure keeps overlay open for retry.
- 2026-06-25: **`.cursorignore`** — excludes `.next/`, `node_modules/`, `scratch/`, generated `integrations/supabase/types.ts`, caches, env files from Cursor indexing (`.codegraph/` kept for CodeGraph MCP).
- 2026-06-25: **Teacher referral rewards** — migration `20260925120000_teacher_referral_rewards.sql`: teacher referrers earn +500 RDM on referred student onboarding (via modified `claim_referral_attribution`, gated on `profiles.role='teacher'`) and +500 more if that student pays via Razorpay within 30 days (new `award_teacher_referral_paid_bonus` RPC hooked in `activate-after-payment`). New `rdm_config` keys `referral_teacher_signup_reward`/`_paid_bonus`/`_paid_window_days`; `referral_attributions` gains `referrer_is_teacher`+`teacher_paid_bonus_awarded_at`. Teacher portal Refer & earn UI + link fixed to `/join?ref=<7hex>`. Student 50/25 + weekly bonus unchanged. Plan: `.cursor/plans/teacher_referral_rewards_2a5cc992.plan.md`. **Migration applied to live DB (TestBee main `bytsiknhtcnlxwzgqkrd`) 2026-06-25 via MCP.**
- 2026-06-25: **Teacher vs student referral tracks** — migration `teacher_to_teacher_referral_track`: `referral_attributions.referee_is_teacher`; teacher→teacher uses `referral_teacher_signup_reward`, teacher→student uses `referral_teacher_student_signup_reward`; Refer & earn UI split hero (teacher) + student section with earned totals from DB.
- 2026-06-25: **Classroom bulk invite rewards** — migration `20260926120000_classroom_bulk_invite_rewards.sql` + bulk invite UI/API: email batch import (20+ new emails → +5,000 RDM once per classroom; +100 per invited student Razorpay paid within 7 days). Tables `classroom_invite_batches`/`classroom_invite_recipients`; RPCs `create_classroom_bulk_invite`, `link_my_classroom_invites`, `award_classroom_batch_paid_bonus` (hooked in `activate-after-payment` + onboarding link). Doc: `.cursor/docs/classroom-bulk-invite-rewards.md`. **Migration applied to live DB 2026-06-25; 8/8 RPC tests pass.**
- 2026-06-25: **Bulk-invite UI on Refer & earn** — hero "Invite your whole class" card + `ClassroomBulkInviteModal` (`components/teacher-portal/classroom/`): classroom picker, add roster (emails/CSV), share join link, and "who joined" tracker (Invited/Joined/Subscribed + RDM earned). New read-only `GET app/api/teacher/classroom/[id]/invite-status` (reads `classroom_invite_recipients`/`_batches` under existing RLS). `ReferEarnView` takes optional `classrooms` prop; passed from `app/teacher-portal/page.tsx`. Frontend-only; flat +5,000 still triggers on roster import (confirmed with user).
- 2026-06-25: **Classroom invite emails** — `lib/email/classroomInviteEmail.ts` + `sendClassroomInviteEmails.ts`; hooked in `POST bulk-invite` after RPC. Sends only for new batch rows; skips already-joined/linked; uses existing SMTP + `transactional_email_logs` (`other`).
- 2026-06-25: **Fix /join code lookup for logged-out users** — RLS blocked anon `classrooms` SELECT; added `GET /api/join/lookup?code=` (admin read, public fields only); `JoinByCodeClient` uses API instead of client Supabase.
- 2026-06-25: **Bulk-invite remove/resend** — `POST bulk-invite/remove` + `POST bulk-invite/resend`; modal row actions Remove (pending only) + Resend. Cleared test invite `temporary8076@gmail.com` for JEE re-test.
- 2026-06-25: **Join code lookup for logged-out email clicks** — migration `20260625180000_lookup_classroom_by_join_code.sql` (RPC `lookup_classroom_by_join_code`, granted to anon); `/join` uses API + RPC fallback. **Applied to live DB.** Requires Vercel deploy for `edublast.vercel.app` email links.
- 2026-06-26: **Teacher Charge Model Phase 1 audit fixes** — IST `slot_at` via `wallClockInTimeZoneToUtc`; true IST month bounds; delivery RDM uses slot duration (`20260930160000` on prod); assignment quota enforced in `createClassroomAssignment`; join-approve server API with fail-closed cap; `npm run supabase:types` safe regen script.
- 2026-06-26: **Live class booking UX** — compact strip (upgrade link / inline book); moved below Enrolled Students on Students tab so roster stays visible.
- 2026-06-27: **Teacher portal sessions UI** — `loadBundle` merges `live_class_slots` into `meetSessions`/`nextSessionLabel`; classroom cards + detail header get **Sessions** pill (dialog lists upcoming); footer/subtitle uses `MeetSessionsStack` for nearest booked slot.
- 2026-06-27: **Teacher Meet host join** — store `profiles.google_calendar_email` on Calendar OAuth; teacher **Start** links use `authuser=` so Meet recognizes organizer (fixes "waiting for host").
- 2026-06-28: **Site tour close snooze** — "Yes, close" on Site Tour confirm now calls `setOnboardingRewardDismissedCooldown()` (1h); was only wired on legacy OnboardingRewardDialog. — (1) `useTeacherPortalData.refresh` in-flight guard collapses poll+focus+visibility+realtime into one bundle load; (2) `loadTeacherPortalBundle` dropped 3 dead queries (unscoped `classroom_members`+nested classrooms-id subquery, unscoped `live_sessions`) always overridden by classroom-scoped reads; (3) 60s TTL cache on `fetchRdmConfig`/`fetchLiveClassDeliveryRdmConfig`/`fetchTeacherRdmCosts` (rdm_config is admin-edited global config).
- 2026-06-28: **Teacher portal GET spam fix** — skip redundant `router.replace` when URL unchanged; reconcile only full-refresh when credits land; removed mount `syncWalletBalance` (wallet open + bundle refresh suffice).
- 2026-06-28: **Admin RDM cleanup (Sankar L)** — deducted 12,000 RDM (16,871→4,871); deleted 6 purchased top-up coupon rows from test/reconcile.
- 2026-06-28: **Assignment completion on teacher cards** — fix URL-encoded `{{POST_ID}}` in task hrefs; repair migration; count mock/past attempts in loadBundle; AssignmentCard shows “Student completed”; checklist links resolve post id at click time.
- 2026-06-28: **Concept Focus teacher completion sync** — teacher portal now reads `student_lesson_mark_completions` (not just stale `profiles.subtopic_engagement`); RLS policy + migration `20260628180000`; removed misleading "Complete" suffix on CF assignment card titles; section-scoped audience in loadBundle.
- 2026-06-28: **Assignment completion rewards payout** — lazy-fund legacy posts; lesson marks in payout; CF mark-complete syncs rewards; re-enabled optional CF completion reward + escrow confirm; `fund-completion-reward` API; auto-pay matching CF posts on lesson mark.
- 2026-06-28: **Teacher assignment live updates** — Supabase Realtime on `classroom_assignment_task_progress` + `classroom_generated_test_attempts` (`20260628140000`); removed 45s/60s assignment polls; cards read bundle directly; detail modal syncs on Realtime bundle refresh.
- 2026-06-28: **Chapter quiz completion fix** — repair migration applied on prod for broken `{{POST_ID}}` hrefs; `resolve-quiz-assignment` API + topic page fallback when URL postId invalid; generated-test-attempt uses parsed task ids for task_progress upsert.
- 2026-06-28: **Concept Focus subtopic unlock** — teacher pays 10×N RDM at publish (`classroom_subtopic_unlock_grants`); student lesson page unlocks all quiz sets + references when grant active; publish confirm in wizard + My Classroom; completion escrow table on prod. Doc: `.cursor/docs/subtopic-unlock-plan.md`.
- 2026-06-28: **Concept Focus RDM cost UX** — `SubtopicUnlockCostPanel` shows per-student unlock + total before create; button "Review & create (X RDM)"; confirm modal "Yes, create (X RDM)" only then publishes.
- 2026-06-28: **Subtopic engagement save storm fix** — lesson focus timer no longer retriggers debounced `saveSubtopicEngagement` every second; timer read from refs, cleanup flush removed, 30s coarse persist + unmount/tab-hide saves.
- 2026-06-28: **Classroom/section RDM labels** — create classroom/section buttons show compact `−N RDM` inline (no confirm popup); section create now charges `create_section` like classroom.
- 2026-06-28: **Student notification copy system** — `lib/teacherPortal/studentNotificationCopy.ts` + bell UI: distinct formats for assignment reminders, nudges, counsel notes, recognition; `[name]` personalization; wizards updated.
- 2026-06-28: **Gyan++ assignment verification** — posting a doubt with `classroomId`/`postId`/`taskId` auto-completes `gyan_engagement` tasks (`doubt_id` on `classroom_assignment_task_progress`); students see posted doubt on done cards; teachers review via assignment detail → Gyan++ Wall highlight (`focusDoubt`). Migration: `20260628210000_classroom_assignment_gyan_doubt_id.sql` (applied remote as `classroom_assignment_gyan_doubt_id`); `integrations/supabase/types.ts` regenerated.
- 2026-06-28: **ClassFeed Active/Done fix** — feed completion now loads `student_lesson_mark_completions` (Concept Focus parity with task-progress API); progress query errors no longer wipe done state; empty posts during load skip done reset.
- 2026-06-28: **ClassFeed session cache (SWR)** — `lib/classroom/studentFeedCache.ts` hydrates posts + Active/Done partition from sessionStorage; skeleton until first partition (no all-posts flash); background revalidate on fetch.
- 2026-06-28: **`npm run build` green** — untyped Supabase client for `doubt_id` progress reads (`task-progress`, `gyan-completions`); `fund-completion-reward` uses `SupabaseClient` cast for `classroom_assignment_completion_rdm_grants` (types not yet regen after migration restore).
- 2026-06-29: **Teacher plan 12/24/60 quotas + RDM fixes** — investor ladder (Free/Starter/Pro included 12/24/60 assignments & live classes); Pro overage 20/assignment + 100/live class; flat publish/schedule fees within quota; `deduct_rdm` NULL fix; live book + `/api/teacher/live-sessions/create` server charges; `teacherPlanQuotaPolicy.ts`; admin `rdm_config` keys; Concept Focus unlock uses `subtopic_unlock_per_student` in UI.
- 2026-06-29: **Student assignment modal UX** — single-column action-first layout; removed side-by-side “How to complete” wall; full-width **Start quiz** CTAs; teacher meta hidden from students.
- 2026-06-29: **Teacher RDM UX (+/− labels, upgrade banners, overage confirms)** — inline `−N`/`+N` on publish & live book buttons; quota upgrade banners at cap; confirm modals before Pro overage (20/100 RDM) and wallet deductions; `teacherRdmUxCopy.ts`, `useTeacherPlanLimits`, `TeacherQuotaUpgradeBanner`, `TeacherLiveClassChargeConfirm`.
- 2026-06-29: **Live class earn-at-schedule** — within monthly cap teachers earn **+100 + 10×roster** on book (no −30 fee); credited immediately via `award_teacher_section_schedule_occurrence_rdm` (`p_force_before_end`); Pro overage still **−100**/extra class; UI `Schedule (+N RDM)` + confirm breakdown; `BookLiveClassSlotPanel` + `live-classes/book`.
- 2026-06-29: **Admin ↔ live class schedule RDM** — `/admin/rdm-table` subsection for schedule earn + quota keys; teacher UI reads `liveClassDelivery` from `/api/teacher/rdm/costs` (admin `rdm_config`: base, per-student, cap); server award RPC uses same keys.
- 2026-06-30: **MCQ sponsorship billing (free students only)** — Concept Focus + chapter quiz sets 2–6 charge teacher `subtopic_unlock_per_student` × free-plan students only; Starter/Pro skipped; `mcqSponsorshipBilling.ts`; UI split in `SubtopicUnlockCostPanel`; student `subscriptionPlan` on teacher bundle.
- 2026-06-30: **Teacher RDM Wallet breakdown** — header popup + `TeacherWalletView` show earn/spend rows from `teacherWalletGuide.ts` (live costs from `/api/teacher/rdm/costs`).
- 2026-06-30: **Student RDM Wallet popup** — header RDM badge opens earn/spend guide (`studentWalletGuide.ts`, `StudentRdmWalletDialog`) instead of `/pricing`; link to Profile → Activity for transaction history.
- 2026-06-30: **Live lesson terminology** — user-facing copy renamed from “live class” to “live lesson” across teacher portal (schedule modal, wallet, quotas, wizard), student rating, subscriptions, and admin RDM table.
- 2026-06-30: **`/api/user/track` 401** — route uses `getSupabaseAndUser` (chunked cookies); anonymous → 204; `PageViewTracker` only fires when signed in.
- 2026-06-30: **`teacher_create_classroom_rdm` = 30** — migration `20260630130000`; wallet/charge read live `rdm_config` (was 28 in DB).
- 2026-06-30: **Mobile app Phase 2** — `mobileapp/`: Subject Chat, Earn & Learn, EduFund, News/Blogs readers, notifications inbox + dashboard bell, Settings; `npx tsc --noEmit` green.
- 2026-06-30: **Mobile app Phase 3** — offline query persist + banner, Expo push token registration (`mobile_push_tokens`, `/api/user/mobile-push-token`), `eas.json` + Play Store build profiles.
- 2026-06-30: **Mobile app Phase 4** — motivation Expo push delivery (`lib/mobile/`), Profile hub (wallet/stats/activity), push tap → notifications.
- 2026-06-30: **Migration applied remote** — `mobile_push_tokens` on TestBee (`bytsiknhtcnlxwzgqkrd`) via Supabase MCP.
- 2026-06-30: **Mobile app completion pass** — checklist deep links, subscription summary, notification action routing, push foreground refresh, README audit; `tsc` green.
- 2026-06-30: **Mobile .env production** — API/web/OAuth bridge → `https://www.edublast.in`; removed LAN + Google client ID from mobile env; `eas.json` preview/production env aligned.