/**
 * Google Analytics 4 measurement ID for the EduBlast Web app.
 * Override with NEXT_PUBLIC_GA_MEASUREMENT_ID when needed (e.g. staging).
 */
export const GA_MEASUREMENT_ID =
  process.env.NEXT_PUBLIC_GA_MEASUREMENT_ID?.trim() || "G-YDE1GM358F";
