/** Secret preview / whitelist sign-in entry (obfuscated path segment). */
export const PREVIEW_AUTH_PATH = "/preview-1006-raknas-amu-tsalbude";

/** Legacy short path — redirect in middleware only. */
export const PREVIEW_AUTH_LEGACY_PATH = "/preview-1006";
